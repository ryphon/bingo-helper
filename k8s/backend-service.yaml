apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: bingo-backend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: bingo-backend
