apiVersion: apps/v1
kind: Deployment
metadata:
  name: bingo-helper
  labels:
    app: bingo-helper
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bingo-helper
  template:
    metadata:
      labels:
        app: bingo-helper
    spec:
      containers:
      - name: bingo-helper
        image: bingo-helper:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
